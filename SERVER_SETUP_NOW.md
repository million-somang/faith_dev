# ⚡ 서버에서 즉시 실행할 명령어

## 🚀 환경 변수 설정 (3분)

```bash
# 1. 최신 코드 받기
cd ~/faith_dev
git pull origin main

# 2. 환경 변수 자동 생성
./setup-env.sh

# 3. dotenv 패키지 설치
npm install dotenv --legacy-peer-deps

# 4. 서버 재시작
pm2 delete all
pm2 start ecosystem.nodejs.config.cjs
pm2 save

# 5. 확인
pm2 list
pm2 logs --lines 20 --nostream

# 6. 테스트
curl http://localhost:3000
```

---

## ✅ 예상 결과

### setup-env.sh 실행 시:

```
🔧 Faith Portal 환경 변수 설정 시작...

✅ .env 파일이 생성되었습니다!

📁 파일 위치: /root/faith_dev/.env
🔐 SESSION_SECRET이 자동으로 생성되었습니다.

📝 생성된 .env 내용:
─────────────────────────────────────
NODE_ENV=production
PORT=3000
DATABASE_PATH=./faith-portal.db
SESSION_SECRET=랜덤생성된32자이상문자열
LOG_LEVEL=info
─────────────────────────────────────

🚀 다음 단계:
   1. pm2 restart faith-portal
   2. pm2 logs faith-portal
```

### PM2 재시작 후:

```
┌────┬─────────────────┬─────────┬─────────┬──────────┬────────┬──────┬──────────┐
│ id │ name            │ mode    │ pid     │ uptime   │ ↺      │ status │ cpu      │
├────┼─────────────────┼─────────┼─────────┼──────────┼────────┼────────┼──────────┤
│ 0  │ faith-portal    │ fork    │ 12345   │ 5s       │ 0      │ online │ 0%       │
└────┴─────────────────┴─────────┴─────────┴──────────┴────────┴────────┴──────────┘

✅ Faith Portal Server is running on http://localhost:3000
```

---

## 🔧 문제 해결

### .env 파일이 이미 있다고 나오면

```bash
# 기존 파일 백업
mv .env .env.backup

# 다시 실행
./setup-env.sh
```

### PM2 프로세스 이름이 다르면

```bash
# 현재 프로세스 확인
pm2 list

# 모두 삭제
pm2 delete all

# 다시 시작
pm2 start ecosystem.nodejs.config.cjs
```

### 권한 오류 발생 시

```bash
# 스크립트 실행 권한 부여
chmod +x setup-env.sh

# 다시 실행
./setup-env.sh
```

---

## 📋 완료 체크리스트

- [ ] `git pull` 완료
- [ ] `./setup-env.sh` 실행
- [ ] `.env` 파일 생성 확인 (`cat .env`)
- [ ] `dotenv` 패키지 설치
- [ ] PM2 재시작
- [ ] PM2 상태 `online` 확인
- [ ] 로그에 에러 없음
- [ ] `curl http://localhost:3000` 정상 응답
- [ ] 회원가입 테스트 (웹 브라우저)

---

## 🎯 회원가입 API 테스트

```bash
# 회원가입 테스트
curl -X POST http://localhost:3000/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test1234",
    "name": "테스트사용자"
  }'
```

성공 시 응답:
```json
{
  "success": true,
  "message": "회원가입이 완료되었습니다.",
  "userId": 1
}
```

---

**모든 명령어를 순서대로 실행하고 결과를 알려주세요!** 😊
